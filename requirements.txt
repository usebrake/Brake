# Detection
nudenet>=3.0.8
onnxruntime>=1.17
opencv-python-headless>=4.9
Pillow>=10.0

# Illustrated/anime NSFW export tooling for source builds.
#
# Normal installer users download a prebuilt ONNX detector package from GitHub
# Releases. Source/dev builds keep these so maintainers can regenerate that
# package from Falconsai/nsfw_image_detection when needed. The long-lived agent
# still runs the classifier on onnxruntime only. torchvision is NOT required.
transformers>=4.30
torch>=2.0
onnx>=1.15

# Capture
mss>=9.0

# Lockout window / guard UI / legacy PyQt fallback
PyQt6>=6.6

# Windows integration
pywin32>=306
keyboard>=0.13.5
psutil>=5.9

# Crypto / state
argon2-cffi>=23.1
cryptography>=42.0

# Config & utils
PyYAML>=6.0
pydantic>=2.5
