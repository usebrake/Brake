# Detection
nudenet>=3.0.8
onnxruntime>=1.17
opencv-python-headless>=4.9
Pillow>=10.0

# Illustrated/anime NSFW (Falconsai/nsfw_image_detection on HuggingFace).
# Optional but recommended — without these the anime_nsfw detector silently
# skips and only photographic NudeNet runs.
#
# These three are INSTALL-TIME ONLY: the "install anime detector" step uses
# them to download the model and export it to an INT8 ONNX file, then they are
# never imported again. The long-lived agent runs the classifier on
# onnxruntime alone (listed above, shared with NudeNet), keeping its resident
# memory low. torchvision is NOT required.
transformers>=4.30
torch>=2.0
onnx>=1.15

# Capture
mss>=9.0

# Lockout window / guard UI / legacy PyQt fallback
PyQt6>=6.6

# Windows integration
pywin32>=306
keyboard>=0.13.5
psutil>=5.9

# Crypto / state
argon2-cffi>=23.1
cryptography>=42.0

# Config & utils
PyYAML>=6.0
pydantic>=2.5
