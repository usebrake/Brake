# Legacy Source Install Checks

Archived 2026-06-18.

Normal users now install Brake with `BrakeSetup.exe` from GitHub Releases. This folder keeps the old source-install checker as backup/reference only.

The installer compatibility scripts under `installer/` are still active and should not be removed without testing a full installer build.
